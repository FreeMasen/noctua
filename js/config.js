// Static configuration constants. No build step, so these are edited by hand.

// Default catalog entry point. Editable at runtime in Settings; this is only the
// seed value on first run. Noctua is catalog-agnostic — nothing else is hardcoded.
export const DEFAULT_CATALOG_URL = "http://minerva.lan/opds";

export const DB_NAME = "noctua";
export const DB_VERSION = 1;

export const APP_NAME = "Noctua";
